@model IEnumerable<ItTutorial.Models.QuizPergunta>

        @foreach (var item in Model)
        {
            
             @Html.Partial("Quizparcial", item)

        }
<input type="submit" onclick="saveState()" value="SUBMIT"  style="align-content:center; margin: 0px;"/>


